# CI Run

This example represents a CI job that emits a Work Receipt for command evidence.

Reviewer reconstruction questions:

1. What command ran?
2. What runtime produced the receipt?
3. What evidence was referenced?
4. Did the run succeed?
5. What artifact can another reviewer validate?

Use the receipt example from `receipt.json`.

